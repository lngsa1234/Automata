function [X] = basicSolutions(A,b)

% obtain dimensions
[m n] = size(A);
p = m -n;

% obtains the column numbers of all nxn matrics out of A

U = ceil(m/p);
V = p;

for i= 1:U
    for j=1:V
    end
end

